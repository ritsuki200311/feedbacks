<!-- 右サイドバー -->
<div style="width: 400px;" class="bg-white rounded-lg shadow-sm border border-gray-200 sticky top-4 max-h-screen overflow-y-auto">
  <!-- 最近の高評価コメント -->
  <div class="p-4 border-b border-gray-200">
    <h3 class="text-lg font-semibold text-gray-800 mb-3 flex items-center gap-2">
      <svg class="w-5 h-5 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"/>
      </svg>
      高評価コメント
    </h3>
    
    <% if @recent_comments&.any? %>
      <div class="space-y-3">
        <% @recent_comments.first(3).each do |comment| %>
          <div class="bg-gradient-to-r from-yellow-50 to-orange-50 rounded-lg p-3 hover:from-yellow-100 hover:to-orange-100 transition-colors border border-yellow-200">
            <div class="flex items-start gap-2">
              <div class="flex-shrink-0">
                <div class="w-8 h-8 bg-yellow-100 rounded-full flex items-center justify-center">
                  <span class="text-sm font-medium text-yellow-700"><%= comment.user.name.first %></span>
                </div>
              </div>
              <div class="flex-1 min-w-0">
                <div class="flex items-center justify-between mb-1">
                  <div class="flex items-center gap-1">
                    <%= link_to comment.user.name, user_path(comment.user), class: "text-sm font-medium text-gray-900 hover:text-blue-600 truncate" %>
                    <span class="text-xs text-gray-500">
                      <%= time_ago_in_words(comment.created_at) %>前
                    </span>
                  </div>
                  <!-- 評価スコア表示 -->
                  <div class="flex items-center gap-1 text-xs">
                    <svg class="w-3 h-3 text-yellow-500" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                    </svg>
                    <span class="font-medium text-yellow-700">
                      +<%= comment.respond_to?(:net_score) && comment.net_score ? comment.net_score : comment.votes.sum(:value) %>
                    </span>
                  </div>
                </div>
                <p class="text-sm text-gray-700 line-clamp-2 mb-1">
                  <%= truncate(comment.body, length: 60) %>
                </p>
                <% if comment.post %>
                  <%= link_to comment.post, class: "text-xs text-blue-600 hover:text-blue-800 hover:underline" do %>
                    「<%= truncate(comment.post.title, length: 20) %>」へのコメント
                  <% end %>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-4">
        <div class="w-12 h-12 bg-yellow-100 rounded-full flex items-center justify-center mx-auto mb-2">
          <svg class="w-6 h-6 text-yellow-500" fill="currentColor" viewBox="0 0 20 20">
            <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
          </svg>
        </div>
        <p class="text-sm text-gray-500">最近高評価なコメントがありません</p>
        <p class="text-xs text-gray-400 mt-1">良いコメントに投票してみましょう！</p>
      </div>
    <% end %>
  </div>

  <!-- 最新のコメント -->
  <div class="p-4 border-b border-gray-200">
    <h3 class="text-lg font-semibold text-gray-800 mb-3 flex items-center gap-2">
      <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
      </svg>
      最新のコメント
    </h3>

    <% if @new_comments&.any? %>
      <div class="space-y-3">
        <% @new_comments.first(3).each do |comment| %>
          <div class="bg-blue-50 rounded-lg p-3 hover:bg-blue-100 transition-colors border border-blue-200">
            <div class="flex items-start gap-2">
              <div class="flex-shrink-0">
                <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center">
                  <span class="text-sm font-medium text-blue-600"><%= comment.user.name.first %></span>
                </div>
              </div>
              <div class="flex-1 min-w-0">
                <div class="flex items-center justify-between mb-1">
                  <div class="flex items-center gap-1">
                    <%= link_to comment.user.name, user_path(comment.user), class: "text-sm font-medium text-gray-900 hover:text-blue-600 truncate" %>
                    <span class="text-xs text-gray-500">
                      <%= time_ago_in_words(comment.created_at) %>前
                    </span>
                  </div>
                  <!-- 新着マーク -->
                  <div class="flex items-center gap-1 text-xs">
                    <span class="bg-blue-500 text-white px-2 py-1 rounded-full text-xs font-medium">NEW</span>
                  </div>
                </div>
                <p class="text-sm text-gray-700 line-clamp-2 mb-1">
                  <%= truncate(comment.body, length: 60) %>
                </p>
                <% if comment.post %>
                  <%= link_to comment.post, class: "text-xs text-blue-600 hover:text-blue-800 hover:underline" do %>
                    「<%= truncate(comment.post.title, length: 20) %>」へのコメント
                  <% end %>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-4">
        <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-2">
          <svg class="w-6 h-6 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
          </svg>
        </div>
        <p class="text-sm text-gray-500">最新のコメントがありません</p>
        <p class="text-xs text-gray-400 mt-1">コメントを投稿してみましょう！</p>
      </div>
    <% end %>
  </div>

  <!-- おすすめのユーザー -->
  <div class="p-4">
    <h3 class="text-lg font-semibold text-gray-800 mb-3 flex items-center gap-2">
      <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
      </svg>
      おすすめのユーザー
    </h3>
    
    <% if @recommended_users&.any? %>
      <div class="space-y-3">
        <% @recommended_users.first(3).each do |user| %>
          <div class="flex items-center gap-3 p-3 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors">
            <div class="flex-shrink-0">
              <div class="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center">
                <span class="text-sm font-medium text-green-600"><%= user.name.first %></span>
              </div>
            </div>
            <div class="flex-1 min-w-0">
              <%= link_to user.name, user_path(user), class: "text-sm font-medium text-gray-900 hover:text-blue-600 block truncate" %>
              <p class="text-xs text-gray-500">
                投稿数: <%= user.posts.size %>件
              </p>
            </div>
            <div class="flex-shrink-0">
              <%= link_to user_path(user), class: "text-xs bg-blue-500 text-white px-2 py-1 rounded hover:bg-blue-600 transition-colors" do %>
                プロフィール
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <p class="text-sm text-gray-500 text-center py-4">おすすめのユーザーがいません</p>
    <% end %>
  </div>
</div>