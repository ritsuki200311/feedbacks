<div class="max-w-7xl mx-auto px-4 py-8">
  <div class="flex gap-4">
    <!-- メインコンテンツエリア -->
    <div class="flex-1 max-w-4xl mx-auto space-y-8">

  <!-- ヘッダー -->


  <!-- ユーザー情報 -->
  <div class="text-gray-700">
    <% if user_signed_in? %>
      <p><strong><%= link_to current_user.name, mypage_path, class: "text-blue-600 hover:underline" %></strong> さん</p>
      <p>保有コイン: <%= current_user.coins %></p>
<p>ランク: <%= current_user.rank %></p>
      <p>メール: <%= current_user.email %></p>
    <% else %>
      <p>ログインしていません。</p>
    <% end %>
  </div>

  <hr class="my-4">

  <!-- 投稿一覧（見出しなし） -->
  <div class="mb-4 border-b border-gray-200">
    <ul class="flex flex-wrap -mb-px text-sm font-medium text-center text-gray-500">
      <li class="mr-2">
        <%= link_to "ホーム", root_path, class: "inline-block p-4 rounded-t-lg hover:border-gray-300 #{'bg-blue-600 text-white' if params[:creation_type].blank? && params[:filter].blank?}" %>
      </li>
      <% if user_signed_in? %>
        <li class="mr-2">
          <%= link_to "フォロー済み", root_path(filter: 'following'), class: "inline-block p-4 rounded-t-lg hover:border-gray-300 #{'bg-blue-600 text-white' if params[:filter] == 'following'}" %>
        </li>
      <% end %>
      <% Post::CREATION_TYPES.each do |key, value| %>
        <li class="mr-2">
          <%= link_to key, root_path(creation_type: value), class: "inline-block p-4 rounded-t-lg hover:border-gray-300 #{'bg-blue-600 text-white' if params[:creation_type].to_s == value.to_s}" %>
        </li>
      <% end %>
    </ul>
  </div>

<% if @posts.any? %>
  <% @posts.each do |post| %>
    <div class="<%= post_card_class(post) %>">
      <p class="text-gray-500 mb-1 text-sm">
        👤 <%= link_to post.user.name, user_path(post.user), class: "text-indigo-600 hover:underline" %>
      </p>

      <h3 class="text-lg font-bold text-indigo-700 mb-1 flex items-center gap-2">
        <%= link_to post.title, post_path(post) %>
        <% if post.is_private? %>
          <span class="bg-red-100 text-red-800 text-xs px-1.5 py-0.5 rounded-full font-medium">🔒</span>
          <% if user_signed_in? && post.user != current_user %>
            <span class="bg-green-100 text-green-800 text-xs px-1.5 py-0.5 rounded-full font-medium">📨</span>
          <% end %>
        <% end %>
      </h3>

      <% if post.request_tag.present? %>
        <p class="text-xs text-gray-600 mb-1">
          <span class="bg-purple-100 text-purple-700 px-1.5 py-0.5 rounded-full text-xs"><%= post.request_tag %></span>
        </p>
      <% end %>

      <p class="mb-2 ml-2 text-sm"><%= truncate(post.body, length: 80) %></p>

      <% if post.tag.present? %>
        <div class="flex flex-wrap gap-1 mb-2">
          <% post.tag_list.first(3).each do |tag| %>
            <span class="bg-blue-100 text-blue-700 text-xs px-2 py-0.5 rounded-full font-medium"><%= tag %></span>
          <% end %>
          <% if post.tag_list.length > 3 %>
            <span class="text-xs text-gray-400">+<%= post.tag_list.length - 3 %></span>
          <% end %>
        </div>
      <% end %>

      <% if post.images.attached? %>
        <div class="flex flex-col items-center mb-2">
          <% post.images.first(1).each_with_index do |image, index| %>
            <div class="relative inline-block mb-2" data-controller="image-comments" data-image-comments-post-id-value="<%= post.id %>" data-image-comments-read-only-value="true">
              <%= image_tag image, class: "w-full max-w-xs mx-auto rounded shadow", data: { "image-comments-target": "image" } %>
              
              <% if post.images.count > 1 %>
                <div class="absolute top-2 right-2 bg-black bg-opacity-70 text-white text-xs px-2 py-1 rounded">
                  +<%= post.images.count - 1 %>
                </div>
              <% end %>
              
              <!-- 画像上のコメントマーカー表示エリア -->
              <div class="absolute inset-0 pointer-events-none" data-image-comments-target="markersContainer"></div>
              
              <!-- マーカー数表示のみ（グローバルコントロールを使用） -->
              <div class="mt-1 flex items-center justify-center">
                <span data-image-comments-target="markerCount" class="text-xs text-gray-500"></span>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>

      <% if post.videos.attached? %>
        <div class="mb-2">
          <% post.videos.first(1).each_with_index do |video, index| %>
            <div class="bg-gray-900 rounded-lg overflow-hidden shadow mb-2 max-w-xs mx-auto">
              <video class="custom-video no-context-menu w-full" 
                     controls 
                     controlsList="nodownload noremoteplaybook"
                     oncontextmenu="return false;"
                     preload="metadata"
                     style="outline: none; height: 200px;">
                <source src="<%= url_for(video) %>" type="video/mp4">
                お使いのブラウザは video タグをサポートしていません。
              </video>
              <% if post.videos.count > 1 %>
                <div class="absolute top-2 right-2 bg-black bg-opacity-70 text-white text-xs px-2 py-1 rounded">
                  +<%= post.videos.count - 1 %>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>

      <% if post.audios.attached? %>
        <div class="mb-2 max-w-xs mx-auto">
          <% post.audios.first(1).each_with_index do |audio, index| %>
            <div class="bg-gradient-to-r from-purple-100 to-blue-100 rounded-lg p-2 mb-2 border border-purple-200">
              <audio class="custom-audio no-context-menu w-full" 
                     controls 
                     controlsList="nodownload noremoteplaybook"
                     oncontextmenu="return false;"
                     preload="metadata"
                     style="outline: none;">
                <source src="<%= url_for(audio) %>" type="audio/mpeg">
                <source src="<%= url_for(audio) %>" type="audio/wav">
                <source src="<%= url_for(audio) %>" type="audio/ogg">
                お使いのブラウザは audio タグをサポートしていません。
              </audio>
              <% if post.audios.count > 1 %>
                <div class="text-center text-xs text-purple-600 mt-1">+<%= post.audios.count - 1 %>件</div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>

      <!-- 投票システム -->
      <% up_count = post.votes.where(value: 1).count %>
      <% down_count = post.votes.where(value: -1).count %>
      <% user_vote = current_user ? (post.votes.find_by(user: current_user)&.value || 0) : 0 %>
      <% total_votes = up_count + down_count %>
      
      <!-- 投票システム（投稿詳細と同じスタイル） -->
      <div class="flex items-center justify-center gap-4 mt-2 pt-2 border-t border-gray-100">
        <!-- プラス投票アイコン（表示のみ） -->
        <div class="<%= user_vote == 1 ? 'text-green-600 bg-green-100 rounded px-1 py-1 font-bold' : 'text-gray-400' %>">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 5v14"/>
            <path d="M5 12h14"/>
          </svg>
        </div>
        
        <!-- 投票バー（詳細と同じスタイル） -->
        <div class="relative w-36 h-6 bg-gray-300 rounded overflow-hidden">
          <% if total_votes == 0 %>
            <!-- 投票がない場合：グレーのバー -->
            <div class="absolute inset-0 flex items-center justify-center text-xs text-gray-700 font-semibold select-none">
              <%= total_votes %>
            </div>
          <% else %>
            <!-- 投票がある場合：カラフルなバーと内訳表示 -->
            <div class="absolute inset-0 flex">
              <% up_percentage = (up_count.to_f / total_votes * 100).round(1) %>
              <% down_percentage = (down_count.to_f / total_votes * 100).round(1) %>
              
              <!-- プラス票のバー -->
              <div class="h-full bg-green-500 relative flex items-center justify-center" style="width: <%= up_percentage %>%">
                <% if up_count > 0 %>
                  <span class="text-xs text-white font-bold"><%= up_count %></span>
                <% end %>
              </div>
              
              <!-- マイナス票のバー -->
              <div class="h-full bg-red-500 relative flex items-center justify-center" style="width: <%= down_percentage %>%">
                <% if down_count > 0 %>
                  <span class="text-xs text-white font-bold"><%= down_count %></span>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
        
        <!-- マイナス投票アイコン（表示のみ） -->
        <div class="<%= user_vote == -1 ? 'text-red-600 bg-red-100 rounded px-1 py-1 font-bold' : 'text-gray-400' %>">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14"/>
          </svg>
        </div>

        <!-- コメント数 -->
        <div class="flex items-center gap-1 text-gray-500">
          <%= link_to post_path(post), class: "flex items-center text-gray-500 hover:text-blue-700" do %>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
              <line x1="9" y1="10" x2="15" y2="10"></line>
              <line x1="9" y1="14" x2="13" y2="14"></line>
            </svg>
            <span class="text-sm font-medium ml-1"><%= post.comments.count %></span>
          <% end %>
        </div>
      </div>

      <!-- 投稿削除ボタン -->
      <div class="text-right mt-2">
        <%= button_to "削除", post_path(post), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "text-red-600 hover:underline" %>
      </div>
    </div>
  <% end %>

<% else %>
  <p class="text-gray-500">まだ投稿がありません。</p>
<% end %>

<!-- ✅ 開発環境限定：ここが一番下 -->
<% if Rails.env.development? %>
  <div class="bg-yellow-100 border border-yellow-400 text-yellow-800 px-4 py-3 rounded mt-8">
    <p class="font-semibold">開発者向け機能</p>
    <p class="mt-1">
      <%= link_to "開発用ユーザー情報一覧", admin_users_path, class: "underline hover:text-yellow-900" %>
    </p>
  </div>
<% end %>

    </div> <!-- メインコンテンツエリア終了 -->

    <!-- 右サイドバー（常時表示） -->
    <div class="block sticky top-4 h-fit">
      <%= render 'layouts/right_sidebar' %>
    </div>
  </div> <!-- flex コンテナ終了 -->
</div> <!-- メインコンテナ終了 -->