<style>
  /* スマホでの横スクロール防止 */
  body {
    overflow-x: hidden;
  }

  * {
    max-width: 100vw;
  }

  img, video, audio {
    max-width: 100% !important;
    height: auto;
  }

  .post-card img {
    max-width: 100% !important;
    object-fit: contain;
  }

  /* 下部ナビゲーション用の極小テキスト */
  .text-2xs {
    font-size: 0.55rem;
    line-height: 0.65rem;
    letter-spacing: -0.025em;
    white-space: nowrap;
  }
</style>
