<h1><%= user.name %>のプロフィール</h1>
<p>メール: <%= user.email %></p>
<h2>投稿一覧</h2>
<% if posts.any? %>
  <% posts.each do |post| %>
    <div class="post">
      <h3>
        <% if user == current_user %>
          <%= link_to post.title, mypage_path %>
        <% else %>
          <%= link_to post.title, user_path(user) %>
        <% end %>
      </h3>
      <p><%= truncate(post.body, length: 100) %></p>
      <small>投稿日: <%= post.created_at.strftime("%Y-%m-%d %H:%M") %></small>
    </div>
    <hr>
  <% end %>
<% else %>
  <p>まだ投稿がありません。</p>
<% end %>